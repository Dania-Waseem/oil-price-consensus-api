# oil-price-consensus-api
A live WTI oil-price feed that pulls from multiple independent sources, cross-verifies them into a single trusted consensus price with confidence/quality scoring and serves it as a standardized JSON API for autonomous agents with PostgreSQL/JSONB storage, TTL-based data lifecycle management and an automated SLA test suite.
